Nothing is outstanding here at the moment.

Everything this file was tracking -- the tabbed window, the Devices page, the
Configuration page with its per-device checkboxes and daemon controls, the
pairing sheet, the device list and `--verbose` in `octomancer status`, and
watching a camera's timecode between writes -- is written. What is written but
has not been watched running is in `doc/KNOWN_ISSUES.md`; that is a different
question from this one, and keeping the two apart is the point of having both
files. This one is for work that has not been done at all.
